//
//  Color+Theme.swift
//  Jarvis
//

import SwiftUI

extension Color {
    /// Deep background — sci‑fi dark.
    static let jarvisBackground = Color(red: 0.04, green: 0.05, blue: 0.12)
    /// Slightly lighter surface for cards.
    static let jarvisSurface = Color(red: 0.08, green: 0.09, blue: 0.18)
    /// Electric blue accent.
    static let jarvisAccent = Color(red: 0, green: 0.48, blue: 1.0)
    /// Cyan for speaking state.
    static let jarvisCyan = Color(red: 0.2, green: 0.8, blue: 0.9)
    /// Green for listening.
    static let jarvisGreen = Color(red: 0.2, green: 0.85, blue: 0.5)
    /// Purple for thinking.
    static let jarvisPurple = Color(red: 0.6, green: 0.4, blue: 1.0)
    /// Idle orb blue.
    static let jarvisIdle = Color(red: 0.2, green: 0.5, blue: 0.9)
    /// Secondary text.
    static let jarvisSecondary = Color(white: 0.55)
}
